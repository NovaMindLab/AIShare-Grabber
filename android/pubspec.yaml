name: image_clip
description: "A new Flutter project."
publish_to: 'none'

version: 1.2.96+196

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Near-field data companion packages
  flutter_blue_plus: ^2.3.2
  flutter_webrtc: ^1.4.1
  mobile_scanner: ^7.2.0
  photo_manager: ^3.9.0
  photo_manager_image_provider: any
  file_picker: ^11.0.2
  device_info_plus: ^12.4.0
  provider: ^6.1.2
  permission_handler: ^11.3.1
  shared_preferences: ^2.5.5
  wifi_iot: ^0.3.18
  mixpanel_flutter: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
